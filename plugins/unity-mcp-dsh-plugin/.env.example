# unity-mcp-dsh-plugin 配置说明模板
#
# 本插件【不需要任何 API 凭证】，此文件仅列出可覆盖的配置项，
# 实际配置请编辑 %DSH_HOME%\profiles\web\cordis.patch.yml 中
# unity-mcp-supervisor 条目的 config 段（安装后即可看到）。

# --- 服务器进程 ---
# command:  uvx 可执行文件路径（默认 C:\Users\kuai\.local\bin\uvx.exe）
# args:     传给服务器的参数（--transport http --http-url http://127.0.0.1:8080
#           --project-scoped-tools）；改端口时需同时改 endpointUrl / url
# cwd:      Unity 项目目录，决定 --project-scoped-tools 的目标项目
# logFile:  服务器 stdout/stderr 日志（默认 %DSH_HOME%\logs\unity-mcp-server.log）

# --- 探活与容错 ---
# endpointUrl:        探活端点（默认 http://127.0.0.1:8080/mcp）
# checkIntervalMs:    探活间隔 ms（默认 10000）
# startupTimeoutMs:   拉起后等待就绪超时 ms（默认 90000）
# maxStartupFailures: 连续启动失败上限（默认 3，超过后暂停自动重启）

# --- 桥接（mcp-unity 条目）---
# url:                http://127.0.0.1:8080/mcp（与服务器端口一致）
# toolCallTimeoutMs:  单次工具调用超时（默认 120000）
