# transcribe_audio / understand_audio 工具所需的 MiMo 凭证模板
#
# 用法：
#   1. 复制本文件为目标电脑上的实际配置文件，填入你的 key：
#        - %DSH_HOME%\.credentials.yaml  （推荐，credentials 服务，格式见下）
#        - 或设置同名环境变量 MIMO_API_KEY
#   2. 工具按 credentials seam → 环境变量 顺序解析，两者任一可用即可。
#
# 注意：不要把真实 key 提交进会被分享/拷贝的仓库或包文件。

# --- 方式 A：credentials 服务（%DSH_HOME%\.credentials.yaml）---
# 把下面两行合并进该文件（不要直接复制整个 .env 进去）：
#
# MIMO_API_KEY: 在这里粘贴你的 Xiaomi MiMo API key
#
# 示例：
# DEEPSEEK_API_KEY: sk-xxxxxxxx
# MIMO_API_KEY: sk-xxxxxxxx

# --- 方式 B：环境变量（PowerShell 临时或用户级）---
# $env:MIMO_API_KEY = "在这里粘贴你的 Xiaomi MiMo API key"
# 或：setx MIMO_API_KEY "在这里粘贴你的 Xiaomi MiMo API key"   （用户级持久）
