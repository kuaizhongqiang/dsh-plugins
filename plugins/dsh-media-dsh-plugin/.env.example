# dsh-media 凭证模板(MIMO_API_KEY + vision 端点)
#
# 工具按 credentials seam → 环境变量 顺序解析,任一可用即可:
#   方式 A(推荐):把下面的行合并进 %DSH_HOME%\.credentials.yaml(不要整文件复制):
#     MIMO_API_KEY: 粘贴你的 Xiaomi MiMo API key
#   方式 B:环境变量  $env:MIMO_API_KEY = "..."   或  setx MIMO_API_KEY "..."
#
# describe-image / document-read 另支持可配置 OpenAI 兼容 vision 端点(默认小米 MiMo)。
#
# 红线:不要把真实 key 提交进任何仓库或包文件。
# DEEPSEEK_API_KEY: sk-xxxxxxxx
# MIMO_API_KEY: sk-xxxxxxxx
